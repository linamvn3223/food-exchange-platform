'use client'
import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
import { authClient } from '@/lib/auth-client'
import { Leaf } from 'lucide-react'
export default function SignUpPage() { const router=useRouter(); const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [type,setType]=useState('Individual'); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const result=await authClient.signUp.email({name,email,password});if(result.error)setError(result.error.message||'Unable to create account');else{router.push('/');router.refresh()}setLoading(false)} return <main className="auth-page"><div className="auth-card"><div className="auth-brand"><span className="logo-mark"><Leaf size={22}/></span><strong>share<span>table</span></strong></div><span className="section-kicker">Join the table</span><h1>Share more good.</h1><p>Create your account and make an impact close to home.</p><form onSubmit={submit}><label>Your name<input value={name} onChange={e=>setName(e.target.value)} required /></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required /></label><label>Password<input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required /></label><label>I’m joining as<select value={type} onChange={e=>setType(e.target.value)}><option>Individual</option><option>Restaurant</option><option>Organization</option></select></label>{error&&<div className="auth-error">{error}</div>}<button className="button button-dark" disabled={loading}>{loading?'Creating account…':'Create account'}</button></form><span className="auth-switch">Already a member? <a href="/sign-in">Sign in</a></span></div></main> }
