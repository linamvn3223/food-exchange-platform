'use client'
import { useState } from 'react'
import { ArrowRight, Camera, Check, Leaf } from 'lucide-react'
import { SiteHeader } from '@/components/site-header'
import { BackHome } from '@/components/back-home'

export default function PostPage() { const [sent,setSent]=useState(false); return <main><SiteHeader /><section className="inner-hero post-hero"><BackHome /><div className="inner-kicker">SHARE THE EXTRA</div><h1>Put it on<br /><em>the table.</em></h1><p>One post can make someone’s day — and keep perfectly good food out of the bin.</p></section><section className="post-form-section"><div className="post-form-intro"><Leaf size={28}/><h2>Tell us what you have.</h2><p>Keep it simple. A clear description helps the right neighbor find you.</p></div>{sent?<div className="success-panel"><span><Check size={25}/></span><h2>Your item is ready to share.</h2><p>Thanks for helping keep good food in the community.</p></div>:<form className="share-form" onSubmit={e=>{e.preventDefault();setSent(true)}}><label>What are you sharing?<input required placeholder="e.g. Fresh garden vegetables" /></label><div className="form-row"><label>Category<select defaultValue=""><option value="" disabled>Select a category</option><option>Fresh produce</option><option>Pantry staples</option><option>Prepared meals</option><option>Bakery & bread</option></select></label><label>Quantity<input required placeholder="e.g. 3 boxes" /></label></div><label>Pickup location<input required placeholder="Neighborhood or landmark" /></label><label>Best before<input type="datetime-local" required /></label><label>Description<textarea required rows={4} placeholder="Anything neighbors should know?" /></label><button className="sample-button" type="submit">Post this item <ArrowRight size={17}/></button><p className="form-note"><Camera size={15}/> You can add photos after posting.</p></form>}</section></main> }
